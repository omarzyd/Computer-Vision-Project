# Outputs

Running the full pipeline writes:

- `output_annotated.mp4` — annotated video with bounding boxes, fall markers, and end-of-clip summary

This folder is gitignored for large binaries. Regenerate by executing `notebooks/tin_can_fall_detection.ipynb`.
